repo: https://github.com/jamesbeedy/layer-puppet-base.git
includes:
  - 'layer:basic'
  - 'layer:apt'
defines:
  puppet-srvc:
    type: string
    default: ''
    description: |
      Puppet pkg aptions are `master`, `agent`, `db`, `ca`.
